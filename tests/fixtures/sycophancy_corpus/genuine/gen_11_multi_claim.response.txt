The p99 is high because 3 things collide. First, the cache invalidates every 30 seconds which forces a re-fetch. Second, the re-fetch runs synchronously on the request thread. Third, the fetch itself takes 200ms in the tail case. Fix: move the invalidation to background and prefetch.